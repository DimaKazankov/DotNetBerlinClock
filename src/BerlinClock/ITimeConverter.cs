namespace BerlinClock
{
    /// <summary>
    /// For me is not obvious what the interface for.
    /// I can't see is going to be different implementation of the algorithm by this reason I've decided to comment it because for this test it's useless.
    /// </summary>

    //public interface ITimeConverter
    //{
    //    string ConvertTime(string aTime);
    //}
}